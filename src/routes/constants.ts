const Root = "/client";

const CARRIERS = `${Root}/carriers`;
const CARRIER_PAGE = `${CARRIERS}/:id`;

const DRIVERS = `${Root}/drivers`;
const DRIVER_PAGE = `${DRIVERS}/:id`;

export const ROUTES = { Root, CARRIERS, CARRIER_PAGE, DRIVERS, DRIVER_PAGE };
